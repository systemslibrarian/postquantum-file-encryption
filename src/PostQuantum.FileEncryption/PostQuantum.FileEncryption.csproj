<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>PostQuantum.FileEncryption</RootNamespace>
    <AssemblyName>PostQuantum.FileEncryption</AssemblyName>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <!-- Trim- and AOT-compatible: enables the trimming/AOT analyzers so we keep it that way. -->
    <IsAotCompatible>true</IsAotCompatible>
    <!-- The library implements the experimental ML-KEM feature internally, so it opts in to its
         own PQFE001 diagnostic. The [Experimental] attribute still flows to external consumers. -->
    <NoWarn>$(NoWarn);PQFE001</NoWarn>
  </PropertyGroup>

  <!-- NuGet package metadata -->
  <PropertyGroup>
    <PackageId>PostQuantum.FileEncryption</PackageId>
    <Version>0.1.0</Version>
    <Title>PostQuantum.FileEncryption</Title>
    <Description>A high-level, delightful, fail-closed file and stream encryptor for .NET. The stable core is symmetric and passphrase-based: authenticated AES-256-GCM containers with chunked streaming and progress, and PBKDF2-HMAC-SHA256 or Argon2id key derivation. AES-256 keeps your data confidential against a quantum (harvest-now-decrypt-later) adversary. An experimental, platform-gated ML-KEM-768 recipient mode is included; the productionized post-quantum public-key path (hybrid X25519 + ML-KEM, multiple recipients) is planned for a separate PostQuantum.FileEncryption.Hybrid package.</Description>
    <PackageReleaseNotes>0.1.0 — first release. Production-ready symmetric (passphrase) engine: AES-256-GCM with PBKDF2 or Argon2id, chunked streaming, progress, cancellation, in-memory and file/stream APIs, fail-closed against tampering/reordering/truncation. ML-KEM-768 recipient mode is included but experimental and platform-gated. 0.x: the on-disk format may change before 1.0.</PackageReleaseNotes>
    <PackageTags>encryption;file-encryption;aes-gcm;aes-256;argon2;pbkdf2;passphrase;authenticated-encryption;cryptography;security;streaming;post-quantum;pqc;ml-kem;dotnet</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageProjectUrl>https://github.com/systemslibrarian/postquantum-file-encryption</PackageProjectUrl>
    <RepositoryUrl>https://github.com/systemslibrarian/postquantum-file-encryption</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
  </PropertyGroup>

  <ItemGroup>
    <None Include="$(MSBuildThisFileDirectory)../../README.md" Pack="true" PackagePath="\" />
    <None Include="$(MSBuildThisFileDirectory)../../LICENSE" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.SourceLink.GitHub" Version="8.0.0" PrivateAssets="All" />
    <PackageReference Include="Konscious.Security.Cryptography.Argon2" Version="1.3.1" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="PostQuantum.FileEncryption.Tests" />
    <InternalsVisibleTo Include="PostQuantum.FileEncryption.Hybrid" />
  </ItemGroup>

</Project>
